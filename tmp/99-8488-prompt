<Context>
You receive a selection in neovim that you need to replace with new code.
The selection's contents may contain notes, incorporate the notes every time if there are some.
consider the context of the selection and what you are suppose to be implementing
<SELECTION_LOCATION>
range(point(3,1),point(6,2))
</SELECTION_LOCATION>
<SELECTION_CONTENT>
type FileExplorerPreviewAPI struct {
	Directory *FileExplorerDirectoryAPI
	Content   []ContentRow
}
</SELECTION_CONTENT>
<FILE_CONTAINING_SELECTION>
package neovim

type FileExplorerPreviewAPI struct {
	Directory *FileExplorerDirectoryAPI
	Content   []ContentRow
}

type FileExplorerDirectoryAPI struct{}

type FileExplorerAPI struct{
	parentDir FileExplorerDirectoryAPI,
	currentDir FileExplorerDirectoryAPI,
	preview FileExplorerPreviewAPI,
}

func (s * Screen) renderFileExplorer(grid *Grid){
	
}
</FILE_CONTAINING_SELECTION>

</Context>
<Prompt>
this type is not quite right. i dont want fields Directory and Content. instead define two separate types that both implement the same interface such that preview can either be []ContentRow or FileExplorerDirectoryAPI
</Prompt>

<Location><File>/home/stefan/Projects/nvim-gui/neovim/file-explorer.go</File><Function>range(point(3,1),point(6,2))</Function></Location>
<FunctionText>type FileExplorerPreviewAPI struct {
	Directory *FileExplorerDirectoryAPI
	Content   []ContentRow
}</FunctionText>
<TEMP_FILE>./tmp/99-8488</TEMP_FILE>
<MustObey>
NEVER alter any file other than TEMP_FILE.
never provide the requested changes as conversational output. Return only the code.
ONLY provide requested changes by writing the change to TEMP_FILE

never attempt to read TEMP_FILE.
It is purely for output.
Previous contents, which may not exist, can be written over without worry
After writing TEMP_FILE once you should be done.  Be done and end the session.

</MustObey>